[package]
name = "denali-core"
version = "0.1.0"
edition = "2024"

[dependencies]
async-trait = "0.1"
pingora = { version = "0.6.0", features = ["openssl", "proxy", "lb", "cache", "time"] }
pingora-limits = "0.6.0"
pingora-runtime = "0.6.0"
